
type PayBlockProps = {
  setCurrentStage: (a: number) => void
  currentStage: number
  n: number
}

export const PayBlock: React.FC<PayBlockProps> = ({setCurrentStage, currentStage, n}) => {
  return <div className=""> PayBlock </div>
} 